Step 1: Do conversion to pressure coordinates in a stand alone script to make sure
        it is possible to interpolate.
Step 2: Implement interpolate_field and write_field



accumlators and storage need to be CuArray

So, I think we need to modify compute! (e.g. compute! and then modify it to be a CuArray instead?)

every compute! is called when first making DiagnosticsHandler

So, this is where I need to store the accumlators and storage

Solutions
1. Modify compute! to also work with all cases, but I need to get the results
2. Well reshape is a free operation anyway (for the most parts)
2. Reshape


Supporting compute! means that we have to put it somewhere.

Maybe fine if we just do it for the accumlators and not the storage
